<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pk.infra.accountclosure.mapper.AccountClosureMapper">

    <select id="countActiveDeletionQueue" resultType="int">
        SELECT COUNT(1)
        FROM user_deleted
        WHERE user_id = #{userId}
          AND status IN ('pending', 'marked')
    </select>

    <insert id="insertDeletionQueue">
        INSERT INTO user_deleted (
            user_id,
            partner_user_id,
            mobile_no,
            reason,
            requested_at,
            status
        ) VALUES (
            #{userId},
            #{partnerUserId},
            #{mobileNo},
            #{reason},
            CURRENT_TIMESTAMP(3),
            'pending'
        )
    </insert>
</mapper>
