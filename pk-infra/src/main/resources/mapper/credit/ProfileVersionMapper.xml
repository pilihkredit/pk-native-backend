<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pk.infra.credit.mapper.ProfileVersionMapper">

    <select id="nextVersionNo" resultType="int">
        SELECT COALESCE(MAX(version_no), 0) + 1
        FROM user_profile_version
        WHERE user_id = #{userId}
    </select>

    <insert id="insert">
        INSERT INTO user_profile_version (
            user_id,
            version_no,
            snapshot_hash,
            snapshot_json,
            completed_modules,
            legal_basis,
            processing_purpose,
            source
        ) VALUES (
            #{userId},
            #{versionNo},
            #{snapshotHash},
            #{snapshotJson},
            #{completedModulesJson},
            #{legalBasis},
            #{processingPurpose},
            #{source}
        )
    </insert>

    <select id="findIdByUserIdAndVersionNo" resultType="long">
        SELECT id
        FROM user_profile_version
        WHERE user_id = #{userId}
          AND version_no = #{versionNo}
    </select>
</mapper>
