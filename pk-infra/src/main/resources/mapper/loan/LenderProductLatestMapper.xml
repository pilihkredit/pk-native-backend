<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pk.infra.loan.mapper.LenderProductLatestMapper">

    <select id="findIdByCreditApplicationId" resultType="long">
        SELECT id
        FROM pk_lender_product_list_latest
        WHERE credit_application_id = #{creditApplicationId}
    </select>

    <insert id="upsertListLatest">
        INSERT INTO pk_lender_product_list_latest (
            profile_id,
            credit_application_id,
            apply_id,
            credit_apply_no,
            lender_user_id,
            credit_status,
            product_status,
            fetched_at
        ) VALUES (
            #{profileId},
            #{creditApplicationId},
            #{applyId},
            #{creditApplyNo},
            #{lenderUserId},
            #{creditStatus},
            #{productStatus},
            #{fetchedAt, typeHandler=com.pk.infra.mybatis.typehandler.InstantTypeHandler}
        )
        ON DUPLICATE KEY UPDATE
            profile_id = VALUES(profile_id),
            apply_id = VALUES(apply_id),
            credit_apply_no = VALUES(credit_apply_no),
            lender_user_id = VALUES(lender_user_id),
            credit_status = VALUES(credit_status),
            product_status = VALUES(product_status),
            fetched_at = VALUES(fetched_at)
    </insert>

    <delete id="deleteUnevenRatesByListLatestId">
        DELETE ur
        FROM pk_lender_product_uneven_rate_latest ur
        INNER JOIN pk_lender_product_repay_method_latest rm ON ur.repay_method_latest_id = rm.id
        INNER JOIN pk_lender_product_latest p ON rm.product_latest_id = p.id
        WHERE p.product_list_latest_id = #{listLatestId}
    </delete>

    <delete id="deleteRepayMethodsByListLatestId">
        DELETE rm
        FROM pk_lender_product_repay_method_latest rm
        INNER JOIN pk_lender_product_latest p ON rm.product_latest_id = p.id
        WHERE p.product_list_latest_id = #{listLatestId}
    </delete>

    <delete id="deleteProductsByListLatestId">
        DELETE FROM pk_lender_product_latest
        WHERE product_list_latest_id = #{listLatestId}
    </delete>

    <insert id="insertProduct" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO pk_lender_product_latest (
            product_list_latest_id,
            product_code,
            product_name,
            min_amount,
            max_amount,
            comprehensive_rate_unit,
            comprehensive_rate,
            sort_order
        ) VALUES (
            #{productListLatestId},
            #{productCode},
            #{productName},
            #{minAmount},
            #{maxAmount},
            #{comprehensiveRateUnit},
            #{comprehensiveRate},
            #{sortOrder}
        )
    </insert>

    <insert id="insertRepayMethod" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO pk_lender_product_repay_method_latest (
            product_latest_id,
            repay_method,
            cycle_type,
            cycle_interval,
            cycle_count,
            total_cycle_interval,
            repay_method_type,
            uneven_bills_repayment_rate_json,
            sort_order
        ) VALUES (
            #{productLatestId},
            #{repayMethod},
            #{cycleType},
            #{cycleInterval},
            #{cycleCount},
            #{totalCycleInterval},
            #{repayMethodType},
            #{unevenBillsRepaymentRateJson},
            #{sortOrder}
        )
    </insert>

    <insert id="insertUnevenRate">
        INSERT INTO pk_lender_product_uneven_rate_latest (
            repay_method_latest_id,
            term_num,
            repayment_rate,
            sort_order
        ) VALUES (
            #{repayMethodLatestId},
            #{termNum},
            #{repaymentRate},
            #{sortOrder}
        )
    </insert>
</mapper>
