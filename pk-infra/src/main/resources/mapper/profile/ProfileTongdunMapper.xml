<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pk.infra.profile.mapper.ProfileTongdunMapper">
    <sql id="tongdunColumns">
        id, profile_id, mobile_no, scene_type, tongdun_key, module_status, request_id,
        last_lender_request_json, last_lender_response_json
    </sql>
    <select id="findByRequestId" resultType="com.pk.infra.profile.repository.ProfileTongdunRow">
        SELECT <include refid="tongdunColumns"/>
        FROM user_profile_tongdun
        WHERE request_id = #{requestId}
        ORDER BY created_at DESC
        LIMIT 1
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO user_profile_tongdun (
            profile_id, mobile_no, scene_type, tongdun_key, module_status, request_id
        ) VALUES (
            #{profileId}, #{mobileNo}, #{sceneType}, #{tongdunKey}, #{moduleStatus}, #{requestId}
        )
    </insert>
    <update id="updateLastLenderAudit">
        UPDATE user_profile_tongdun
        SET last_lender_request_json = #{requestDataJson},
            last_lender_response_json = #{responseDataJson}
        WHERE request_id = #{requestId}
    </update>
</mapper>
