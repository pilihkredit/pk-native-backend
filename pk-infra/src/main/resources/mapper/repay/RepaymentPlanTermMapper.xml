<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pk.infra.repay.mapper.RepaymentPlanTermMapper">
<resultMap id="termMap" type="com.pk.core.repay.port.RepaymentPlanTermRepository$TermRecord">
<constructor><arg column="id" javaType="long"/><arg column="loan_application_id" javaType="long"/>
<arg column="loan_apply_id" javaType="string"/><arg column="bill_no" javaType="string"/>
<arg column="sub_bill_no" javaType="string"/><arg column="term_no" javaType="int"/>
<arg column="term_status" javaType="string"/>
<arg column="due_date" javaType="java.time.Instant" typeHandler="com.pk.infra.mybatis.typehandler.InstantTypeHandler"/>
<arg column="grace_date" javaType="java.time.Instant" typeHandler="com.pk.infra.mybatis.typehandler.InstantTypeHandler"/>
<arg column="schd_amount" javaType="java.math.BigDecimal"/><arg column="should_amount" javaType="java.math.BigDecimal"/>
<arg column="paid_amount" javaType="java.math.BigDecimal"/><arg column="overdue_days" javaType="int"/>
<arg column="amount_detail_json" javaType="string"/>
<arg column="last_repay_time" javaType="java.time.Instant" typeHandler="com.pk.infra.mybatis.typehandler.InstantTypeHandler"/>
<arg column="synced_at" javaType="java.time.Instant" typeHandler="com.pk.infra.mybatis.typehandler.InstantTypeHandler"/></constructor></resultMap>
<insert id="upsertTerm">INSERT INTO repayment_plan_term (loan_application_id,loan_apply_id,bill_no,sub_bill_no,term_no,term_status,due_date,grace_date,schd_amount,should_amount,paid_amount,overdue_days,amount_detail_json,last_repay_time,synced_at)
VALUES (#{loanApplicationId},#{loanApplyId},#{billNo},#{subBillNo},#{termNo},#{termStatus},#{dueDate,typeHandler=com.pk.infra.mybatis.typehandler.InstantTypeHandler},#{graceDate,typeHandler=com.pk.infra.mybatis.typehandler.InstantTypeHandler},#{schdAmount},#{shouldAmount},#{paidAmount},#{overdueDays},#{amountDetailJson},#{lastRepayTime,typeHandler=com.pk.infra.mybatis.typehandler.InstantTypeHandler},#{syncedAt,typeHandler=com.pk.infra.mybatis.typehandler.InstantTypeHandler})
ON DUPLICATE KEY UPDATE loan_apply_id=VALUES(loan_apply_id),bill_no=VALUES(bill_no),sub_bill_no=VALUES(sub_bill_no),term_status=VALUES(term_status),due_date=VALUES(due_date),grace_date=VALUES(grace_date),schd_amount=VALUES(schd_amount),should_amount=VALUES(should_amount),paid_amount=VALUES(paid_amount),overdue_days=VALUES(overdue_days),amount_detail_json=VALUES(amount_detail_json),last_repay_time=VALUES(last_repay_time),synced_at=VALUES(synced_at)</insert>
<select id="findByLoanApplicationId" resultMap="termMap">SELECT id,loan_application_id,loan_apply_id,bill_no,sub_bill_no,term_no,term_status,due_date,grace_date,schd_amount,should_amount,paid_amount,overdue_days,amount_detail_json,last_repay_time,synced_at FROM repayment_plan_term WHERE loan_application_id=#{loanApplicationId} ORDER BY term_no ASC</select>
<select id="findPendingByProfileId" resultMap="termMap">SELECT rpt.id,rpt.loan_application_id,rpt.loan_apply_id,rpt.bill_no,rpt.sub_bill_no,rpt.term_no,rpt.term_status,rpt.due_date,rpt.grace_date,rpt.schd_amount,rpt.should_amount,rpt.paid_amount,rpt.overdue_days,rpt.amount_detail_json,rpt.last_repay_time,rpt.synced_at FROM repayment_plan_term rpt INNER JOIN loan_application la ON la.id=rpt.loan_application_id WHERE la.profile_id=#{profileId} AND rpt.term_status IN ('UNPAID','OVERDUE') ORDER BY rpt.due_date ASC,rpt.term_no ASC</select>
</mapper>
